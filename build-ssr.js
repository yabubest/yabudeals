// build-ssr.js - Rendert die neuesten Deals statisch in index.html vor
const fs = require('fs');
const path = require('path');

console.log('Building SSR HTML...');
// Platzhalter-Skript: Kann bei Bedarf erweitert werden
process.exit(0);
